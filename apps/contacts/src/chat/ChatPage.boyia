class ChatPage extends ViewDoc {
	prop msgWindow = null;
	prop chatEdit = null;
	fun initView() {
		if (!this.msgWindow) {
        	this.msgWindow = this.itemByID(ViewGroup, "msg_window");
        	this.chatEdit = this.itemByID(ViewGroup, "chat_edit");
        }
	}
}